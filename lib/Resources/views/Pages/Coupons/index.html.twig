{% extends "@VSApplication/layout.html.twig" %}

{# Import Macros #}
{% import "@VSApplication/Macros/alerts.html.twig" as alerts %}
{% import "@VSApplication/Macros/translations.html.twig" as vs_translations %}
{% from "@VSApplication/Macros/pagination.html.twig" import pagination %}

{% block title %}{{ parent() }} :: {{ 'vs_payment.template.coupon_index_title' | trans( {},'VSPaymentBundle' ) }}{% endblock %}

{% block pageTitle %}<i class="icon_genius"></i> {{ 'vs_payment.template.coupon_index_title' | trans( {},'VSPaymentBundle' ) }}{% endblock %}

{% block content %}
    <div class="row">   
        <div class="col-lg-12">
            <section class="panel">
                <table class="table table-striped table-advance table-hover vsTableSortable">
                    <thead>
                        <tr>
                            <th width="50">N</th>
                            <th data-sortable="true" data-field="code">{{ 'vs_payment.template.coupon_index_code' | trans( {},'VSPaymentBundle' ) }}</th>
                            <th data-sortable="true" data-field="name">{{ 'vs_payment.template.coupon_index_name' | trans( {},'VSPaymentBundle' ) }}</th>
                            <th data-sortable="true" data-field="type">{{ 'vs_payment.template.coupon_index_type' | trans( {},'VSPaymentBundle' ) }}</th>
                            <th>{{ 'vs_payment.template.coupon_index_pricing_plan' | trans( {},'VSPaymentBundle' ) }}</th>
                            <th data-sortable="true" data-field="amountОff">{{ 'vs_payment.template.coupon_index_amount_off' | trans( {},'VSPaymentBundle' ) }}</th>
                            <th data-sortable="true" data-field="percentOff">{{ 'vs_payment.template.coupon_index_percent_off' | trans( {},'VSPaymentBundle' ) }}</th>
                            <th data-sortable="true" data-field="enabled">{{ 'vs_payment.template.coupon_index_valid' | trans( {},'VSPaymentBundle' ) }}</th>
                            <th>{{ 'vs_application.template.items_index_row_translations' | trans( {},'VSApplicationBundle' ) }}</th>
                            <th width="150">{{ 'vs_payment.template.action' | trans( {},'VSPaymentBundle' ) }}</th>
                        </tr>
                    </thead>
                    <tbody>
                    {% for item in resources %}
                        <tr>
                            <td>{{ loop.index }}</td>
                            <td>{{ item.code }}</td>
                            <td>{{ item.name }}</td>
                            <td>{{ item.type }}</td>
                            <td>{% if item.pricingPlan %}{{ item.pricingPlan.title }}{% endif %}</td>
                            <td>{% if item.amountOff %}{{ item.amountOff }} {{ item.currency | vs_currency_symbol }}{% endif %}</td>
                            <td>{% if item.percentOff %}{{ item.percentOff }} %{% endif %}</td>
                            <td style="text-align: center;">
                                {% if item.valid %}
                                    <i class="fas fa-check" style="color: #82c91e;"></i>
                                {% else %}
                                    <i class="fas fa-times" style="color: #ff1e1e;"></i>
                                {% endif %}
                            </td>
                            <td>{{ vs_translations.flags( translations[item.id] ) }}</td>
                            <td>
                                <div class="btn-group">
                                    <a class="btn btn-primary"
                                        href="{{ path('vs_payment_coupon_update', {'id': item.id}) }}"
                                        title="{{ 'vs_application.template.items_index_row_action_edit' | trans( {},'VSApplicationBundle' ) }}"
                                    >
                                        <i class="fas fa-edit"></i>
                                    </a>
                                    <a class="btn btn-danger btnDeleteResource"
                                        href="{{ path('vs_payment_coupon_delete', {'id': item.id}) }}" 
                                        data-csrfToken="{{ csrf_token( item.id ) }}" 
                                        title="{{ 'vs_application.template.items_index_row_action_delete' | trans( {},'VSApplicationBundle' ) }}"
                                    >
                                        <i class="icon_close_alt2"></i>
                                    </a>
                                </div>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="3">
                                {{ alerts.info( 'vs_application.template.there_are_no_items_to_display' | trans( {},'VSApplicationBundle' ) ) }}
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </section>
            
            {% if resources.haveToPaginate() %}
                {{ pagerfanta( resources, null, { 'proximity': 10 } ) }}
            {% endif %}
        </div>
    </div>
    
    {% include '@VSApplication/Partial/resource-delete.html.twig' %}
{% endblock %}

{% block head_styles %}
    {{ parent() }}
    {{ encore_entry_link_tags( 'js/coupons-index' ) }}
{% endblock %}

{% block body_scripts %}
    {{ parent() }}
    
    {{ encore_entry_script_tags( 'js/coupons-index' ) }}
{% endblock %}
