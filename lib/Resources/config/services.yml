imports:
    - { resource: services/controller.yaml }
    - { resource: services/form.yaml }
    - { resource: services/menu.yaml }
    - { resource: services/payment.yaml }

parameters:
    #vs_payment.gateway_factories: ['offline', 'paypal_express_checkout', 'paypal_pro_checkout', 'stripe_checkout', 'stripe_js']
    vs_payment.gateway_factories: ['offline']
    
services:
    _defaults:
        autowire: true
        autoconfigure: true
        public: true

